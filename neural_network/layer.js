

class Layer 
{
    constructor(nodes, activation_function, layer_type) 
    {
      self.nodes = nodes;
      //Have an enum or object of some sort with activation function type pointing to a particular activation function formula
    }

}